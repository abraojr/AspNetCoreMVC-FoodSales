@model IEnumerable<SalesFood.Models.Food>

<div>
    <h2>All Foods</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var food in Model)
        {
            <div class="col-sm-4 col-md-4 col-lg-4">
                <p><img src="@food.ImageUrl" class="img-fluid" /></p>
                <h3>Price : @food.Price.ToString("c")</h3>
                <h4>@food.Name</h4>
                <p>@food.ShortDescription</p>
            </div>
        }
    </div>
</div>